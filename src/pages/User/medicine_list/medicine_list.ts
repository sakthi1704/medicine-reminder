import { Component } from '@angular/core';
import { NavController } from 'ionic-angular';

@Component({
  selector: 'page-about',
  templateUrl: 'medicine_list.html'
})
export class medicineListPage {
  constructor(public navCtrl: NavController) {
  }

}
